# Capture from the PDM microphone. The AUDIOMIX handoff is required: the M7 can
# only drive the audio clock tree once Linux has given the domain up.
CONFIG_IMX8M_M7_AUDIOMIX_BOOT_HANDOFF=y
CONFIG_PINCTRL=y
CONFIG_AUDIO=y
CONFIG_AUDIO_DMIC=y
