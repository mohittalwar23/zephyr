CONFIG_AUDIO_DMIC_NATIVE_SIM=y
CONFIG_I2S_NATIVE_SIM=y
CONFIG_AUDIO_CODEC_DUMMY=y

# Deep host queues so the TX prebuffer cushion is not capped by the driver
CONFIG_I2S_NATIVE_SIM_QUEUE_SIZE=32
CONFIG_AUDIO_DMIC_NATIVE_SIM_QUEUE_SIZE=32
